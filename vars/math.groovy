def sum(a,b)
{
  echo the sum of ${a} and &{b} is ${a+b}"
}
def sub(a,b)
{
  echo the subtraction resultof of ${a} and ${b} of ${a-b}"
}
def mul(a,b)
{
  echo the product of ${a} and ${b} of ${a*b}"
}
def div(a,b)
{
  echo the devition of ${a} and ${b} of ${a/b}"
